# Workflows placeholder

CI workflows can be added after repository initialization.
Recommended checks:

- validate JSON manifests;
- scan for Git LFS pointer files in deliverable ZIPs;
- verify no broken `img src` in `out.html` and `object.html`;
- verify Source active manifest matches repository source layout.
