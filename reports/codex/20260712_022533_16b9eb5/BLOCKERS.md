# Blockers

No delivery blocker.

Known non-blocking infrastructure defect: direct file preview and Windows screenshot setup fail with windows sandbox helper_unknown_error: setup refresh had errors. The in-memory image inspection path passed and was used for all required visual/layout QA.
